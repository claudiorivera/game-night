import React from "react";
import { Container } from "@material-ui/core";
import { GameList } from "../components";

const Home = () => {
  return (
    <Container>
      <GameList />
    </Container>
  );
};

export default Home;
