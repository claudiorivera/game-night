import React from "react";
import { Container } from "@material-ui/core";
import { GameDetails } from "../components";

const Home = () => {
  return (
    <Container>
      <GameDetails />
    </Container>
  );
};

export default Home;
