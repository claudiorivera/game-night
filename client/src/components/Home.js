import React from "react";
import { Container } from "@material-ui/core";
import { AddGame } from "../components";

const Home = () => {
  return (
    <Container>
      <AddGame />
    </Container>
  );
};

export default Home;
